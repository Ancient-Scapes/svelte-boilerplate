block variables

doctype html
html(lang="ja")
  head
    meta(charset="utf-8")
    title #{title}
    meta(name="viewport", content="width=device-width, initial-scale=1")
    link(rel="stylesheet", type="text/css", href="../css/main.css")
    //- dirNameが宣言されたとき同じ名前のpages配下のcssを読み込む / ないときは読み込まない
    #dirName
      if dirName
        - const cssPath = "../css/" + dirName + "/style.css";
        link(rel="stylesheet", type="text/css", href=cssPath)
body
  block content
  block scripts